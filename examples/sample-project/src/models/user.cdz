// User model module
module User {
    type UserData {
        id: int,
        name: string,
        email: string
    }
    
    pure function isValid(user: UserData) -> bool {
        return user.name != "" && user.email != ""
    }
    
    export { UserData, isValid }
}