// Simple FlowLang Development Server
// Basic version that works with current transpiler capabilities

function start_server(port: int) -> int {
    return port + 1000
}

function parse_port(port_str: string) -> int {
    // Simplified - would parse string to int
    return 8080
}

function main() -> int {
    let port = parse_port("8080")
    let result = start_server(port)
    return result
}